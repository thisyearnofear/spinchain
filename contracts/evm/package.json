{
  "name": "spinchain-evm-contracts",
  "version": "1.0.0",
  "description": "SpinChain Avalanche smart contracts",
  "main": "deploy.js",
  "scripts": {
    "deploy": "hardhat run --network fuji deploy.js",
    "compile": "hardhat compile"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.2"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^2.0.0",
    "ethers": "^6.16.0",
    "hardhat": "^2.0.0"
  }
}
